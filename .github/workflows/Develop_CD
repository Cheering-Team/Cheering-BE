spring:
  datasource:
    url: jdbc:mysql://cheering-rds.cx8u2y8waky9.ap-northeast-2.rds.amazonaws.com:3306/Cheering?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: iamanywhere1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
