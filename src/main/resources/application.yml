spring:
  profiles:
    active: local
    group:
      local: local, common, secret
      blue: blue, common, secret
      green: green, common, secret

server:
  env: blue

---

spring:
  config:
    activate:
      on-profile: local

  data:
    redis:
      host: localhost
      port: 6379
      repositories:
        enabled: false

server:
  port: 8080
  serverAddress: localhost

serverName: local_server

---

spring:
  config:
    activate:
      on-profile: blue

  data:
    redis:
      host: blue_redis
      port: 6379
      repositories:
        enabled: false

server:
  port: 8080
  serverAddress: 15.165.150.47


serverName: blue_server

---

spring:
  config:
    activate:
      on-profile: green

  data:
    redis:
      host: green_redis
      port: 6379
      repositories:
        enabled: false

server:
  port: 8081
  serverAddress: 15.165.150.47

serverName: green_server

---

spring:
  config:
    activate:
      on-profile: common
